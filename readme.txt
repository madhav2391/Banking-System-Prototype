single.c is used to create only the first account in singleusers.txt 
joint.c is used to create only the first account in jointusers.txt
admin.c is used to create only the first account in adminusers.txt
note for all the above we need to give only 1 as user id
header.h contains all the structs used.
client.c acts as client. options promt on client side for user to select their choice.
server.c acts as server.
